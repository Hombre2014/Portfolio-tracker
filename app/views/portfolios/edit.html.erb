<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing portfolio</h1>

  <%= render "update", portfolio: @portfolio, class:"contents" %>

  <%= link_to "Show this portfolio", "/users/#{current_user.id}/portfolios/show", class: "ml-2 rounded-lg py-3 px-5 bg-cyan-300 inline-block font-medium" %>
  <%= link_to "Back to portfolios", "/users/#{current_user.id}/portfolios", class: "ml-2 rounded-lg py-3 px-5 bg-cyan-500 inline-block font-medium" %>
</div>
